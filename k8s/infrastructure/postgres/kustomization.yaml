apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: odo-core

resources:
  - service.yaml
  - statefulset.yaml

commonLabels:
  app: postgres
  component: database
  environment: development
