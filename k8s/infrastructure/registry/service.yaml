apiVersion: v1
kind: Service
metadata:
  name: docker-registry
  namespace: odo-core
  labels:
    app: docker-registry
spec:
  type: NodePort
  ports:
  - port: 5000
    targetPort: 5000
    nodePort: 32000
    protocol: TCP
    name: registry
  selector:
    app: docker-registry