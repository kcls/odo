apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: odo-core

resources:
  - configmap.yaml
  - pvc.yaml
  - service.yaml
  - deployment.yaml

commonLabels:
  app: docker-registry
  component: registry
  environment: development