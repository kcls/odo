apiVersion: v1
kind: Service
metadata:
  name: odo-notify
  namespace: odo-pub
  labels:
    app: odo-notify
    part-of: odo
spec:
  type: NodePort
  selector:
    app: odo-notify
  ports:
  - name: http
    port: 8080
    targetPort: http
    nodePort: 30044
    protocol: TCP
